import {ScrollView} from 'react-native';
import React from 'react';
import Card from '../../components/Card';

const Home = () => {
    return (
        <ScrollView>
            <Card />
            <Card />
            <Card />
            <Card />
            <Card />
        </ScrollView>
    );
};

export default Home;
